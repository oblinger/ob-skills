# RULESET R-diagram-folder
description:: Diagram authoring + validation. Umbrella
includes:: [[R-diagram]]

> [!info] Naming note
> The folder is `R-diagram/` and contains `R-diagram.md` (the umbrella 22-rule set). The folder-file convention would normally name itself `R-diagram.md`, but that name is taken by the umbrella content. This file (`R-diagram-folder.md`) is the folder-file stub; the umbrella set ([[R-diagram]]) is what most adopters reference directly. When F132 factoring lands, this collapses naturally — `R-diagram.md` shrinks to a thin includes:: list and this folder-file may be deleted.
