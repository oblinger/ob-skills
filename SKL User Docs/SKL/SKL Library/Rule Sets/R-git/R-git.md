# RULESET R-git
description:: Git-discipline rule sets — commit cadence, PR workflow, branch protection. Adopt the umbrella to pull all git rule sets.
includes::

(Placeholder — no rule sets curated yet. Future candidates: `R-commit-discipline` (commit-on-transition, no amend), `R-pr-workflow`, `R-no-force-main`.)
