# RULESET R-code
description:: Code-flavored rule sets — language- or platform-specific coding conventions. Adopt the umbrella to pull all code rule sets, or cherry-pick individual sets.
includes:: [[mac-app]]

Currently one set materialized: [[mac-app]] (macOS app development — code signing, TCC, sandboxing). Future candidates: `R-rust`, `R-python`, `R-typescript`, `R-shell`.
