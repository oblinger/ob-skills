---
description: "SUPERSEDED — doc-publishing conventions now live in FCT Documentation Site. Retained as a redirect until F191 sweeps CAB links."
---

# CAB Documentation Publishing

> [!info] Superseded by [[FCT Documentation Site]] (F218, 2026-06-29)
> The live facet covers all of this — and more (the canonical MkDocs-Material stack, two-tier project-page/full-site model, three-output-format pattern):
>
> - **Private `{NAME} Docs/` vs published `docs/` split**, **`docs/` folder taxonomy** (user/ dev/ api/), **generator chooser** (MkDocs/TypeDoc/docc), **MkDocs setup**, **build→preview→deploy workflow** → all in **[[FCT Documentation Site]]** (+ [[code-publish]] for the workflow).
> - **Stray "Documenting Code Interfaces" snippet** (return-type annotations, aligned inline `#` comments) — a *code-style* tip, not a publishing convention (CAB itself flagged it illustrative-only); belongs in a language/code-style discipline (e.g. `DSC rust` / [[DSC markdown]] code-block conventions), not preserved here.
>
> This stub is kept only so inbound `[[CAB Documentation Publishing]]` links resolve; **[[SKA Backlog#^F191|F191]]** will repoint them and remove the holding folder.
