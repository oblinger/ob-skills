---
description: "SUPERSEDED — markdown-formatting rules now live in DSC markdown (+ the md skill for file-tree/TOC forms). Retained as a redirect until F191 sweeps CAB links."
---

# CAB Markdown Formatting

> [!info] Superseded by [[DSC markdown]] (F218, 2026-06-29)
> Drained into the live discipline + skill — nothing load-bearing lost:
>
> - **Vertical spacing, definition lists, em-dash, fenced-block rules, table rules** → **[[DSC markdown]]** (already there).
> - **Fullwidth `＃` (U+FF03) Python-comment workaround** + **figure-space (U+2007) mechanics** (incl. "Edit tool can't match figure spaces — use Python") → **folded into [[DSC markdown]] § Mechanical rules** this pass.
> - **File-tree forms** + **TOC forms** (and the `cab-toc.py` → `md-toc.py` script) → the **[[md]]** skill: [[md-file-tree]], [[md-toc]]. (The legacy `[[CAB File Tree Format]]` / `[[CAB TOC Format]]` sub-docs migrate alongside.)
> - **Date format (`YYYY-MM-DD` ISO-8601) + reverse-chron dated sections** → conventions of **[[FCT Log]]** (dated filenames/sections) + the Inbox/Features facets; the file-naming date patterns are in **[[FCT Naming]] § Exception B**.
>
> This stub is kept only so inbound `[[CAB Markdown Formatting]]` links resolve; **[[SKA Backlog#^F191|F191]]** will repoint them and remove the holding folder.
