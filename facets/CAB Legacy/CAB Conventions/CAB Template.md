---
description: "SUPERSEDED — the template convention now lives in FCT Template (+ Files / Folders / Variables children). Retained as a redirect until F191 sweeps CAB links."
---

# CAB Template

> [!info] Superseded by [[FCT Template]] (F220, 2026-07-01)
> Drained into the authored facet — nothing load-bearing lost:
>
> - **Template-IS-an-instance rule + smoke test** → [[FCT Template]] R-template-01 / R-template-07.
> - **Anatomy (exemplar / about / Variables)** → [[FCT Template Files]]; the `---` divider is **revised** to the `✂ ──── template notes ──── ✂` cut-line (R-template-08 — a bare `---` was ambiguous with frontmatter and horizontal rules).
> - **Variables + no-data rule** → [[FCT Template Variables]] (extended: two placeholder forms `{{UPPER_SNAKE}}` vs `{{Mixed Case one-off}}`, R-template-02; inline vs spanning braces, R-template-09).
> - **Cumulative-vs-structural placeholders** → **revised** in R-template-03: a repeating section now ships a *variableized entry-pattern + a level-marked `...`*, not a bare header (the header-only rule taught nothing about the shape).
> - **Two kinds (file / folder)** → [[FCT Template Files]] / [[FCT Template Folders]] (folder templates gained the unified-variable-namespace rule, R-template-10).
> - **Naming `_{Name} Template`** → R-template-04, sharpened: the middle IS the instance-name pattern and is usually variableized (a constant middle is flagged).
> - **Two locations (central library / inline)** → the central library is **dropped** (F220 Q3 — templates live in the folder they govern); dispatch-row integration → R-template-05/06.
> - **Audit categories** (`template-is-spec`, `template-has-fake-cumulative-entries`, `missing-folder-template-row`, `orphan-template`) → carried verbatim into the R-template rules.

# BRIEF

- **Redirect stub only** — the live spec is [[FCT Template]] + its three children. Don't re-grow content here; this file exists so legacy `[[CAB Template]]` links resolve until [[SKA Backlog#^F191|F191]] sweeps them.
