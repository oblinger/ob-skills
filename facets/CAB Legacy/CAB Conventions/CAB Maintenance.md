---
description: "SUPERSEDED — the at-rest validation checklist is now enforceable audit rules across the FCT specs + the /audit system. Retained as a redirect until F191 sweeps CAB links."
---

# CAB Maintenance

> [!info] Superseded (F218, 2026-06-29)
> The at-rest checklist became enforceable rules — each item now lives as a `check::`/`stated` rule the `/audit` system runs, not a prose checklist:
>
> - **ANCHOR PAGE — all docs linked** → `R-dispatch-table-07` (the catch-all marker guarantees every doc in the folder surfaces) + [[FCT Anchor Page]].
> - **ROADMAP — high-level only** → [[FCT Roadmap]] (`R-roadmap` shape rules).
> - **LINK TABLE — rows valid** → [[FCT Dispatch Table]] (`R-dispatch-table` cell/link rules; `/audit dispatch`).
> - **slug INDEX — slug registered** → [[ATL Slugs]] + `cab-slug-scan`.
>
> The "what an anchor looks like at rest" role is now the union of the FCT facet rulesets, checked mechanically by `/audit`. Nothing unique remains as prose.
>
> This stub is kept only so inbound `[[CAB Maintenance]]` links resolve; **[[SKA Backlog#^F191|F191]]** will repoint them and remove the holding folder.
