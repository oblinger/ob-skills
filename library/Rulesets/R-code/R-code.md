# RULESET R-code
description:: Code-flavored rulesets — language- or platform-specific coding conventions. Adopt the umbrella to pull all code rulesets, or cherry-pick individual sets.
include:: [[R-mac]]

Currently one set materialized: [[R-mac]] (macOS app development — code signing, TCC, sandboxing). Future candidates: `R-rust`, `R-python`, `R-typescript`, `R-shell`.
