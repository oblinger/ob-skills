# RULESET R-process
description:: Process rulesets — feature lifecycle, verification tiers, state transitions. Adopt the umbrella to pull all process rulesets.
includes::

(Placeholder — no rulesets curated yet. Future candidates: `R-feature-lifecycle` (Designing → Ready → Active → Verify → Done), `R-verification-tiers`.)
