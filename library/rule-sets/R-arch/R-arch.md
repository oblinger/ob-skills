# RULESET R-arch
description:: Architecture rule sets — patterns for code organization, module structure, dependency direction. Adopt the umbrella to pull all architecture rule sets, or cherry-pick individual sets.
includes::

(Placeholder — no rule sets curated yet. Future candidates: `R-factory-pegboard` per F108, `R-interfaces-folder`, `R-single-source-of-truth`.)
